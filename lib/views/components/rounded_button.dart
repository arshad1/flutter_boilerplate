import 'package:flutter/material.dart';

class RoundedButton extends StatelessWidget {
  String image;
  Function callback;
  String name;

  RoundedButton({this.image, this.name, this.callback});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
